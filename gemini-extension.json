{
  "name": "string-web-access",
  "version": "1.0.7",
  "description": "Search the web, fetch or send requests to any URL, and map a site's URLs as clean, LLM-ready Markdown for Gemini CLI.",
  "contextFileName": "GEMINI.md",
  "settings": [
    {
      "name": "API Key",
      "description": "Your String Web Access API key, from portal.usestring.ai",
      "envVar": "STRING_API_KEY",
      "sensitive": true
    }
  ],
  "mcpServers": {
    "string-web-access": {
      "httpUrl": "https://mcp.usestring.ai/v1/mcp",
      "headers": { "Authorization": "Bearer ${STRING_API_KEY}" }
    }
  }
}
